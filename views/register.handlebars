<main class="container mt-4">
    <h2>Sign up</h2>
    <form action="/register" method="post" id="registration-form" class="mt-3">
        <div class="mb-3">
            <label for="emailAddressInput" class="form-label">Email address</label>
            <input type="email" id="emailAddressInput" name="emailAddressInput" class="form-control"
                value="{{postData.emailAddressInput}}">
            <div class="form-text" id="email-info" hidden>Email field value must be in a proper email address format
            </div>
        </div>
        <div class="mb-3">
            <label for="ageInput" class="form-label">Age</label>
            <input type="number" id="ageInput" name="ageInput" class="form-control" value="{{postData.ageInput}}">
            <div class="form-text" id="age-info" hidden>Age field value must be a whole number between 18 and 100</div>
        </div>
        <div class="mb-3">
            <label class="form-label" for="passwordInput">Password</label>
            <input type="password" id="passwordInput" name="passwordInput" class="form-control"
                value="{{postData.passwordInput}}">
            <div class="form-text" id="password-info" hidden>
                <ul>
                    <li>Password must be at least 8 characters long.</li>
                    <li>Password must contain at least one uppercase character.</li>
                    <li>Password must contain at least one number.</li>
                    <li>Password must contain at least one special character.</li>
                    <li>Password cannot contain space.</li>
                </ul>
            </div>
        </div>
        <div class="mb-3">
            <label class="form-label" for="confirmPasswordInput">Confirm Password</label>
            <input type="password" id="confirmPasswordInput" name="confirmPasswordInput" class="form-control"
                value="{{postData.confirmPasswordInput}}">
            <div class="form-text" id="confirmpassword-info" hidden>Confirm password and password must match.</div>
        </div>
        <div class="mb-3">
            <label class="form-label" for="roleInput">Select role</label>
            <select class="form-select" name="roleInput" id="roleInput">
                <option value="admin">Admin</option>
                <option value="user">User</option>
            </select>
            <div class="form-text" id="role-info" hidden>You must select a role from the given options in the dropdown.
            </div>
        </div>
        <div class="mb-3">
            <label class="form-label" for="preferGenreInput" id="preferGenreLabel" hidden>Select genres</label>
            <select class="form-select" name="preferGenreInput" id="preferGenreInput" multiple hidden>
                <option value="action">Action</option>
                <option value="adventure">Adventure</option>
                <option value="comedy">Comedy</option>
                <option value="drama">Drama</option>
                <option value="fantasy">Fantasy</option>
                <option value="horror">Horror</option>
                <option value="musicals">Musicals</option>
                <option value="mystery">Mystery</option>
                <option value="romance">Romance</option>
                <option value="science fiction">Science fiction</option>
                <option value="sports">Sports</option>
                <option value="thriller">Thriller</option>
                <option value="western">Western</option>
            </select>
            <div class="form-text" id="genre-info" hidden>
                <ul>
                    <li>You must select a genre from the given options in the dropdown.</li>
                    <li>To select multiple genres keep the Ctrl key pressed while selecting.</li>
                </ul>
            </div>
        </div>
        <div class="mb-3">
            <label class="form-label" for="preferContentInput" id="preferContentLabel" hidden>Set preferred content</label>
            <select class="form-select" name="preferContentInput" id="preferContentInput" hidden>
                <option value="G">G(General Audience)</option>
                <option value="PG">PG</option>
                <option value="PG-13">PG-13</option>
                <option value="R">R</option>
                <option value="18+">18+(NC 17)</option>
            </select>
            <div class="form-text" id="content-info" hidden>
                You must select prefer content from the given options in the dropdown.
            </div>
        </div>
        <button type="submit" class="btn btn-primary">Submit</button>
        <div id="client-side-error-div" hidden></div>
    </form>
    {{#if hasError}}
    <p class="error">{{error}}</p>
    {{/if}}
    <a href="/login">Already have an account? Click here to log-in</a>
    <script src="/public/js/register_form.js"></script>
</main>