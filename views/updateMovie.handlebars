<!DOCTYPE html>
<html lang="en">

<head>
    <meta charset="UTF-8" />
    <meta name="viewport" content="width=device-width, initial-scale=1.0" />
    <title>Update Movie Information</title>
    <link href="https://cdn.jsdelivr.net/npm/bootstrap@5.3.2/dist/css/bootstrap.min.css" rel="stylesheet"
        integrity="sha384-T3c6CoIi6uLrA9TneNEoa7RxnatzjcDSCmG1MXxSR1GAsXEV/Dwwykc2MPK8M2HN" crossorigin="anonymous" />
    <link href="/public/css/styles.css" rel="stylesheet">
</head>

<body>
    <div class="container">
        <ul class="breadcrumbs">
            <li class="breadcrumbs__crumb">
                <a class="a-link-normal" href="/admin">admin homepage</a>
            </li>
            <li class="breadcrumbs__crumb breadcrumbs__crumb--divider">›</li>
            <li class="breadcrumbs__crumb breadcrumbs__crumb--current">
                <span class="a-color-state">Update Movie</span>
            </li>
        </ul>
    </div>
    <div class="container mt-5">
        <h2>Update Movie Information</h2>
        <div id="error-message" class="alert alert-danger" style="display: none;"></div>
        <form id="updateMovieForm" action="/admin/toUpdate/{{movie._id}}" method="post" enctype="multipart/form-data">
            <div class="mb-3">
                <label for="movieName" class="form-label">Title:</label>
                <input type="text" class="form-control" id="movieName" name="title" value="{{movie.title}}" required />
            </div>
            <div class="mb-3">
                <label for="director" class="form-label">Director:</label>
                <input type="text" class="form-control" id="director" name="director" value="{{movie.artists.director}}"
                    required />
            </div>

            <div class="mb-3">
                <label for="actors" class="form-label">Actors:</label>
                <input type="text" class="form-control" id="actors" name="actors" value="{{movie.artists.actors}}"
                    required />
            </div>
            <div class="mb-3">
                <label for="writer" class="form-label">Writer:</label>
                <input type="text" class="form-control" id="writer" name="writer" value="{{movie.artists.writer}}" />
            </div>
            <div class="mb-3">
                <label for="producer" class="form-label">Producer:</label>
                <input type="text" class="form-control" id="producer" name="producer" value="{{movie.artists.producer}}"
                    required />
            </div>
            <div class="mb-3">
                <label for="releaseDate" class="form-label">ReleaseDate:</label>
                <input type="text" class="form-control" id="releaseDate" name="releaseDate"
                    value="{{movie.releaseDate}}" required />
            </div>


            <div class="mb-3">
                <label for="contentRating">contentRating:</label>
                <select name="contentRating" id="contentRating">
                    <option value="G" {{#if movie.ratingOption.G}}selected{{/if}}>G</option>
                    <option value="PG" {{#if movie.ratingOption.PG}}selected{{/if}}>PG</option>
                    <option value="PG-13" {{#if movie.ratingOption.PG13}}selected{{/if}}>PG-13</option>
                    <option value="R" {{#if movie.ratingOption.R}}selected{{/if}}>R</option>
                    <option value="NC-17" {{#if movie.ratingOption.NC17}}selected{{/if}}>NC-17</option>
                </select>
            </div>

            <div class="mb-3">
                <label for="genre" class="form-label">Genres (press command to select more genres):</label>
                <select class="form-select" name="genre[]" id="genre" multiple>
                    {{#each genresWithSelection}}
                    <option value="{{this.name}}" {{#if this.selected}}selected{{/if}}>{{this.name}}
                    </option>
                    {{/each}}
                </select>
            </div>

            <div class="mb-3">
                <label for="imageUpload" class="form-label">Upload a new thumbnail:</label>
                <input type="file" class="form-control" id="imageUpload" name="image" accept="image/*"
                    value="{{movie.thumbnail}}" required />
            </div>

            <div class="mb-3">
                <button type="submit" class="btn btn-primary">Update</button>
            </div>
        </form>
    </div>

    <script src="https://code.jquery.com/jquery-3.6.0.min.js"></script>
    <script src="https://cdn.jsdelivr.net/npm/bootstrap@5.3.2/dist/js/bootstrap.bundle.min.js"></script>
    <script src="/public/js/admin_client_side_validation.js"></script>
</body>

</html>